mkdir mugesh-mywebapp && cd mugesh-mywebapp
# create server.js, package.json, Dockerfile, Jenkinsfile (paste contents)
git init
git add .
git commit -m "initial commit - mugesh demo app"
# create remote repo on GitHub (you can create via GitHub UI)
git remote add origin git@github.com:mugesh/my-webapp-cicd.git
git push -u origin main
